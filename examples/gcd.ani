// Euclidean GCD via a recursive method

world MathGcd {
  awaken() {}

  run(x, y) { kaeru this.gcd(x, y) }

  gcd(x, y) {
    geass y == 0 {
      kaeru x
    } counter {
      kaeru this.gcd(y, x % y)
    }
  }
}

jutsu m = summon MathGcd()
creation(m.run(5023427, 920311))
