// One method passes different value kinds through

world Identity {
  awaken() {}
  of(x) { kaeru x }
}

jutsu idn = summon Identity()
creation(idn.of(3))
creation(idn.of("hello"))
creation(idn.of(truth))
